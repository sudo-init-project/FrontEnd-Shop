import React from 'react';

const Footer = () => {
  const footerStyle = {
    
    textAlign: 'center',
  };

  return (
    <footer style={footerStyle}>
      <p className="text-body-secondary">@ AyiGroup 2024</p>
    </footer>
  );
};

export default Footer;